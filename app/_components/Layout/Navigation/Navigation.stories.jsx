import Navigation from "./Navigation";

const meta = {
  title: "Layout/Navigation",
  component: Navigation,
};

export default meta;

export const Default = () => <Navigation />;
export const MobileMenuOpen = () => <Navigation />;
